import { useEffect } from "react";
import "./CheckCode.css";

export const CheckCode = () => {
    // Funcion que se encarga del dat del formulario + reenvio de formulario

    const formSubmit = async (formData) => {};

    const handleResend = () => {};

    // useEffect que van a gestionar las respuetas y errores error.jsx

    useEffect(() => {}, []);

    useEffect(() => {}, []);

    // Condicionales que evaluan los stados de navegacion

    return (
        <>
            <p>checkCode</p>
        </>
    )
}